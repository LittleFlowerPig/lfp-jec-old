server:
  port: 49999

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10000/eureka/

spring:
  application:
    name: jec-config-nati
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/properties/
