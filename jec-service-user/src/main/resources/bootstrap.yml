server:
  port: 20101

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:10000/eureka/

spring:
  application:
    name: jec-service-user
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: jec-config-git